import LoginForm from "@/components/login/LoginForm";
import React from "react";

const Home = async () => {
    return (
      <main>
        <LoginForm />
      </main>
    );
};

export default Home;